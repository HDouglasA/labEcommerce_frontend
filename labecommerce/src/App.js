import { GlobalStyle } from './GlobalStyle';
import { Router } from './Router';

export const App = () => {
    return (
        <div>
            <GlobalStyle/>
            <Router/>
        </div>
    )
}

export default App;
